{
  "name": "bend-mobile",
  "description": "Experimental native iOS and Android UI framework for Bend",
  "license": "Apache-2.0",
  "private": true,
  "bun-create": {
    "postinstall": "/usr/bin/env bun run bootstrap",
    "start": "bun run build"
  },
  "scripts": {
    "bootstrap": "bun run doctor && bun run setup && bun run build",
    "doctor": "bun scripts/doctor.ts",
    "setup": "bun scripts/setup.ts",
    "build": "bun scripts/build.ts",
    "test": "bun scripts/build.ts && bun tests/bootstrap.ts && bun tests/smoke.ts && bun tests/kernel.ts && bun run test:cpu",
    "test:cpu": "clang++ -std=c++17 -pthread tests/native-cpu.cpp native/cpu.cpp -o dist/native-cpu-test && ./dist/native-cpu-test"
  }
}
